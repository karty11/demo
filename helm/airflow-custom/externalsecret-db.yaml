apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: airflow-db-externalsecret
  namespace: airflow
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: aws-secrets-store
    kind: ClusterSecretStore
  target:
    name: bankapp-db-externalsecret   # k8s Secret created in airflow ns
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: bankapp/mysql
        property: username
    - secretKey: password
      remoteRef:
        key: bankapp/mysql
        property: password
